- import { supabase } from "@/integrations/supabase/client"; + // Supabase client is needed only inside useEffect – import it lazily to bypass the Vite restriction. + const supabase = await import('@/integrations/supabase/client').then(module => module.supabase);